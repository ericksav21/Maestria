base = sys.argv[1]
fit, _ = ut.data_fitness(os.path.join(path, methods_dir[0]), base)
file = open(base + ".fitnessEvolution", "w")
x = []
y = []
for k, v in fit.items():
	x.append(k)
	y.append(v)
	file.write(str(k) + " " + str(v) + "\n")

file.close()
ut.make_plot(x, y, base + "_fitness")
