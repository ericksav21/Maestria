Para compilar: 'make' sin comillas.

Para ejecutar: './main [Grado del polinomio]' sin comillas.

Ejemplo de ejecución: './main 3'

Se generarán los archivos de los puntos de la muestra y los puntos generados por el polinomio generado. Y se imprimirán los coeficientes.

Para generar la gráfica: 'gnuplot inst.gpl'.
